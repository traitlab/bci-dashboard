labelbox
requests
python-dotenv
pyyaml
Pillow

# 15c_aggregate_survey.py only (the --aggregate path of 15xi). The dashboard
# itself is stdlib.
numpy
pandas
# Not on PyPI: install both from the traitlab repo, or run labelling/rank_queue.py
# on a checkout that has them (its docstring says how). Only labelling/ imports
# them; dashboard/ reads the JSON and CSV they leave behind.
labelfirst
speciesfirst
